# vim: set ts=2 sw=2 tw=0 ft=yaml:
---
single:
  type: node
  pool: default

  image: {{ image }}

  user:
    {{ user }}:
      password: {{ password }}
    root:
      password: linux

  ssh:
    copy-key:
      users:
        - {{ user }}
        - root
